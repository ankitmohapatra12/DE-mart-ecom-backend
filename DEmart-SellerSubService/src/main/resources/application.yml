server:
  port: 1004
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/DEmart-Ecom
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    


    
    
#given name 
  application:
    name: DEmart-SellerSubService
    
    
    
    
    

    
#EUREKA CONFIGURATION

eureka:
  instance:
    prefer-ip-address: true
  client: 
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka